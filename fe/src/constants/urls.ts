export enum AUTHN_URLS {
  Login = "/api/auth/login",
  Logout = "/api/auth/logout",
  Signup = "/api/users",
}

export enum USER_URL {
  GetUsers = "/api/users",
  CreateUsers = "/api/users",
  DeleteUsers = "/api/users",
}

export enum ARTICLE_URL {
  GetArticles = "/api/articles",
  CreateArticles = "/api/articles",
  DeleteArticles = "/api/articles",
  UpdateArticles = "/api/articles",
}
